# notion-cursor

日出日落倒计时在 [`sunrise-countdown/`](sunrise-countdown/)。

多台电脑同步方式：GitHub 为唯一真相。云端 Agent 只改这个仓库；每台 Windows clone 同一仓库，计划任务 `git pull` 后运行 `sunrise-countdown.ahk`。窗口位置等本机 ini 不同步。详见该目录 README。
